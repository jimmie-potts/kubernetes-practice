# Python for the Forge Console, Go for the later operator

The Forge Console is FastAPI plus HTMX using the official Python Kubernetes client, because the Console is plumbing and the learning target is Kubernetes itself. A late stretch phase adds a Tenant CRD reconciled by a Go operator, since Go and client-go are the ecosystem's native tools and worth learning once the core concepts have landed.
